// Iron Screens — Media Renderer (shared fallback — web-safe)
// Platform-specific versions: MediaRenderer.native.tsx / MediaRenderer.web.tsx
// This file should not be reached in normal builds.
export { default } from './MediaRenderer.web';
